export interface Track {
  url: string;
  title?: string;
  artist?: string; // Người biểu diễn
  album?: string;
  genre?: string; // Thể loại
  date?: string;
  artwork?: string; // Hình ảnh
  duration?: string; // Thời gian
}